import path from 'path'

const incomingPath = path.join(process.cwd(), '../../data/incoming/')
export const incomingVideoPath = path.join(incomingPath, '/video/')
export const incomingAudioPath = path.join(incomingPath, '/audio/')

export const uploadsPath = path.join(process.cwd(), '../../data/uploads/')
